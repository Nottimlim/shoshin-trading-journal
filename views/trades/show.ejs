<h1 class="text-2xl font-bold mb-4">Trade Details</h1>

<div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
  <p><strong>Symbol:</strong> <%= trade.symbol %></p>
  <p><strong>Entry Price:</strong> $<%= trade.entryPrice.toFixed(2) %></p>
  <p><strong>Entry Date:</strong> <%= trade.entryDate.toLocaleDateString() %></p>
  <p><strong>Exit Price:</strong> <%= trade.exitPrice ? `$${trade.exitPrice.toFixed(2)}` : 'N/A' %></p>
  <p><strong>Exit Date:</strong> <%= trade.exitDate ? trade.exitDate.toLocaleDateString() : 'N/A' %></p>
  <p><strong>Profit/Loss:</strong> <span class="<%= trade.profitLoss >= 0 ? 'text-green-600' : 'text-red-600' %>"><%= trade.profitLoss ? `$${trade.profitLoss.toFixed(2)}` : 'N/A' %></span></p>
  <p><strong>Trade Type:</strong> 
    <% if (trade.tradeType === 'long') { %>
      Long 
      <svg class="w-6 h-6 text-green-500 inline-block" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M3 17L9 11L13 15L21 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M14 7H21V14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    <% } else { %>
      Short 
      <svg class="w-6 h-6 text-red-500 inline-block" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M3 7L9 13L13 9L21 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M14 17H21V10" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    <% } %>
  </p>
  <p><strong>Risk/Reward Ratio:</strong> <%= trade.riskRewardRatio %></p>
  <p><strong>Notes:</strong> <%= trade.notes %></p>
</div>

<div class="flex space-x-4">
  <a href="/trades/<%= trade._id %>/edit" class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded">Edit</a>
  <form action="/trades/<%= trade._id %>?_method=DELETE" method="POST">
    <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" onclick="return confirm('Are you sure you want to delete this trade?')">Delete</button>
  </form>
  <a href="/trades" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">Back to Trades</a>
</div>
