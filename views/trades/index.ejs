<h1 class="text-2xl font-bold mb-4">Your Trades</h1>

<a href="/trades/new" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-4 inline-block">Add New Trade</a>

<% if (trades.length === 0) { %>
  <p>No trades yet. Start by adding a new trade!</p>
<% } else { %>
  <table class="min-w-full bg-white">
    <thead>
      <tr>
        <th class="py-2 px-4 border-b">Symbol</th>
        <th class="py-2 px-4 border-b">Entry Date</th>
        <th class="py-2 px-4 border-b">Entry Price</th>
        <th class="py-2 px-4 border-b">Exit Price</th>
        <th class="py-2 px-4 border-b">R/R</th>
        <th class="py-2 px-4 border-b">Outcome</th>
        <th class="py-2 px-4 border-b">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% trades.forEach(trade => { %>
        <tr>
          <td class="py-2 px-4 border-b"><%= trade.symbol %></td>
          <td class="py-2 px-4 border-b"><%= trade.entryDate.toLocaleDateString() %></td>
          <td class="py-2 px-4 border-b"><%= trade.entryPrice %></td>
          <td class="py-2 px-4 border-b"><%= trade.exitPrice || 'Open' %></td>
          <td class="py-2 px-4 border-b"><%= trade.riskRewardRatio %></td>
          <td class="py-2 px-4 border-b"><%= trade.outcome %></td>
          <td class="py-2 px-4 border-b">
            <a href="/trades/<%= trade._id %>" class="text-blue-500 hover:text-blue-700">View</a>
            <a href="/trades/<%= trade._i
